<div class="mt-3">
  <div class="card-list-container-fluid pt-3">
    <div class="row">
      <% items.each do |item| %>
        <div class="card mr-2 mt-4 shadow-lg">
          <%= link_to item_path(item) do %>
            <%= attachment_image_tag item, :image, :fill, 200, 150 , bclass:"card-img-top py-2 px-2";%>
          <% end %>
          <div class="card-body">
          <div class="card-title"><%= item.name %></div>
            <div>¥<%= item.add_tax_included_price.to_s(:delimited) %></div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>